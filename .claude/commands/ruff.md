Lint and format my Python and React/TypeScript code.

First, find and fix Python errors with the following:
```bash
cd /project
ruff check --fix .
ruff format .
```
Follow Python PEP 8 and use type hints.

Finally, commit changes
